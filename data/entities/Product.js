export default class Product {
  constructor(name, value, notes, categoryId, id) {
    this.name = name;
    this.value = value;
    this.notes = notes;
    this.categoryId = categoryId;
    this.id = id;
  }
}
